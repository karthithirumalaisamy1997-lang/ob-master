// Not used directly — the OB app (src/ob-app.html) already includes its own
// storage handling: it uses window.storage when available (Claude.ai
// preview), and automatically falls back to the browser's localStorage when
// running as a normal deployed website. This file is kept only so the
// project's folder layout matches your existing structure.
export {};
