Put your app source files here (main.js, components, styles, etc).

This project currently has a single-file version of the app (ob-app.html)
that can be dropped straight into index.html without any build step at all,
if you'd rather skip Vite entirely. Ask Claude if you want that version instead.
