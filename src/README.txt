This folder is kept for future modularization (splitting the app's
JS/CSS out of index.html). The app currently works fully self-contained
inside index.html, so nothing here is required for it to run.
