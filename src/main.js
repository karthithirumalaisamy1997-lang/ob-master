// This app is a single self-contained file (index.html has all its HTML, CSS,
// and JS inline) so there's nothing for this file to mount or import.
//
// It's kept here only so the project has a src/main.js, matching a normal
// Vite project layout. `npm run dev` / `npm run build` both work fine as-is —
// Vite just serves/copies index.html directly, this file isn't referenced
// and doesn't need to be.
