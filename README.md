Reserved for future modularization (splitting `index.html`'s inline
`<script>`/`<style>` into separate JS/CSS modules). Not required for the app
to run — everything currently lives in the root `index.html`.
